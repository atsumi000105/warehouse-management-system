# Use postgres/example user/password credentials
version: '3.1'

services:
    db:
        image: postgres
        environment:
            POSTGRES_PASSWORD: dobaman
            POSTGRES_DB: dobaman
        ports:
            - 5432:5432